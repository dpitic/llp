ROOT = ..
PLATFORM = $(shell $(ROOT)/systype.sh)
include $(ROOT)/Make.defines.$(PLATFORM)

PROGS = mappings_loop segfault_badaddr mmap

all:	$(PROGS)

%:	%.asm $(LIBLLP)
	$(AS) $(ASFLAGS) $@.asm
	$(LD) $@.o -o $@ $(LDLIBS)

clean:
	rm -f $(PROGS) $(TEMPFILES) *.o

.PHONY:	clean
